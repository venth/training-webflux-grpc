date_and_time=[%d{yyyy-MM-dd HH:mm:ss.SSS}]
hostname=[${HOSTNAME}]
thread_name=[%thread]
log_level=%level
logger_name=%logger{36}
message=%msg
new_line=%n

log_pattern=${date_and_time} ${hostname} ${thread_name} ${log_level} ${logger_name} - ${message}${new_line}